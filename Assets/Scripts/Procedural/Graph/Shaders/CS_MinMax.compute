

Texture2D<float> _Tex;
RWStructuredBuffer<float2> _Bounds : register(u0);

[numthreads(8, 8, 1)]
void CSMain(uint2 pos : SV_DispatchThreadID) {

    float val = tex[pos];

    InterlockedMin(bounds[0].min, val);
    InterlockedMax(bounds[0].max, val);
}